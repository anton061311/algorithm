[SECTION 07: Sorting and Searching(정렬, 이분검색과 결정 알고리즘)]

[목차]
  1. 선택정렬 - 26.02.11 완
   key point:
        - 선택 정렬: i와 j를 정해두고 가장 작은 수의 index와 i를 바꿈.
        - 최솟값을 제일 앞으로 보내는 정렬
        - 최솟값 찾아 교환, 반에서 1등 세워놓기
        - 남은 애들 중 가장 작은 애들부터 앞에 두면서 정렬
        - idx 선언

  2. 버블정렬 - 26.02.11 완
   key point:
        - 버블 정렬: 인접한 두 수를 비교 후 판단 기준에 따라 바꿈
        - 옆 비교 후 큰 값 밀기, 거품이 위로 올라감
        - 제일 큰 놈만 뒤로 감
        - 매 번 맨앞부터 비교

  3. 삽입정렬 - 26.02.11 완
   key point:
        - 뭔 좆같은 정렬이지?
        - 걍 밀리는 정렬 정도로 알고 있어도 될듯
        - 자리에 끼워 넣기, 카드 정렬,
        - 1,2번째와 3번째를 비교, 정렬된 나라를 하나 씩 확장
        - 올리고>비교하고>밀고>넣고

  4. LRU(캐시, 카카오 변형) - 26.02.11 완
   key point:
        - 감히 역순으로 복붙, case 분류
        - 정수형 변수만으로 통해 boolean flag 역할과 index 역할 수행
        ! cache[i] = cache[i-1] 형태로 밀어야 안전하고 깔끔함.

  5. 중복확인 - 26.02.11 완
   key point:

  6. 장난꾸러기 - 26.02.11 완, 좋은 접근법
   key point:
        - int[] tmp = arr. clone(); // arr를 tmp에 복사함
        - 그냥 다른 하나의 배열을 복제한 다음 비교하면 더 쉬웠던 문제

  [ㅋ]7. 좌표 정렬(compareTo) - 26.02.11 완
   key point:
        - ArrayList 객체에 임의의 Point 클래스를 넣음
        - 오버라이드, implement, 생성자, Collection.sort() 등 많은 개념이 요구되는 문제
        - compareTo()

  8. 이분검색 - 26.02.11 완
   key point:
        - 순차탐색은 O(n) 이지만 이분 탐색은 이보다 빠름. 방법은 변수 lt,rt 선언 후 탐색.
        - 고3 수리논술에서 했던 그것ㅇㅇ

  [RE] 9. 뮤직비디오(결정 알고리즘) - 26.02.12 해설 참조
   key point:
        - Arrays.stream() 에 대해..
        - 배열을 하나의 변수에 다 더하려면 Arrays.stream(arr).sum(); ㄱㄱ
        - 최댓값 최솟값에 대한 two pointers를 설정하는 것이 핵심임
        - 결정알고리즘은 이분 검색을 요구하는 것
        - lt부터 rt 사이에 요구하는 답이 무조건 존재하는 확신이 있을 때 이분 검색을 사용

        - 26.02.12 해설 봄. 병렬적 사고를 요구하는 문제,
        * lt(9) -------------- rt(45)
        * mid일 때, M장 보다 더 필요한지, 덜 필요한지?
        * M과 비교 될 정수를 반환하는 메서드를 작성하자. 중복이 되니~
        * count 함수의 파라미터는 mid, arr 이고, 내부 변수는 cnt, 도합, 리셋하는 알고리즘 구현
        * 씨발 3장 이하니까 1,2장까지 포함이구나, 범위 잘 신경 쓸것

  [RE] 10. 마구간 정하기(결정 알고리즘) - 26.02.12 해설 참조
   key point:
   “최소 거리를 d 이상으로 하면서 C마리를 배치할 수 있나?”
    가능하면 d를 더 키울 수 있고
    불가능하면 d를 줄여야 함
    이게 딱 결정 알고리즘 + 이분 탐색 패턴.

    헷갈린 “최댓값/최솟값” 정리
    문제에서 최솟값 = “배치된 말들 중 가장 가까운 거리”
    우리가 구하는 건 그 최솟값을 최대화한 값
    즉,
    내부는 min
    바깥은 max

[다시 풀어볼 문항]
- 9
- 10
